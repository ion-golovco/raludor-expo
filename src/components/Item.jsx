import React, { Component } from 'react';

import Sorturi from "../assets/sorturi"

const Sorts = Sorturi

class Item extends Component {
    state = {  }
    render() { 
        return ( <div>
            {Sorts.map((n)=>{n.})}
        </div> );
    }
}
 
export default Item;